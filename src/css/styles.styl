@import "normalize.css"
@import "colors"
@import "smart-grid.styl"

@font-face
	font-display sans-serif
	font-family 
	src url() format("woff")

html
	height 100%

img
	max-width 100%
	
